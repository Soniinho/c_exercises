#include <stdio.h>
#include <stdlib.h>

int main()
{

    /*
    double n1, n2, m;
    printf("Digite 2 numeros:");
    scanf("%lf%lf", &n1, &n2);
    if(n2 == 0){
        printf("Impossivel dividir por 0");
    }
    else {
        m = n1* n2;
        printf("Multiplicação: %.lf", m); //%.1lf 1 casa dps da virg
    }

    return 0;
    */

    /*
    char sexo; //%c
    char nome[30]; //%s
    int x; //%d
    float area; //%f
    double z; //%lf
    */

    /*
    double n1, n2n m;
    printf("Digite dois numeros:");
    scanf("%lf%lf", &n1, &n2);
    m = n1 * n2;
    printf("Multiplicação: %lf,", m);

    return 0;
    */

    /*
    float n1, n2n m;
    printf("Digite dois numeros:");

    scanf("%f", &n1); //scanf("%f%f", &n1, &n2);
    scanf("%f", &n2);

    m = n1 * n2;
    printf("Multiplicação: %f,", m);

    return 0;
    */
}
