#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <math.h>

int main()
{

/*
    float cata, catb,hipotenusa;
    printf ("digite o valor do cateto A:");
    scanf ("%f",&cata);
    printf ("digite o valor do cateto B:");
    scanf ("%f",&catb);
    //hipotenusa = sqrt(a*a+b*b);
    hipotenusa = sqrt(pow(cata,2)+ pow(catb,2));
    printf ("\nO valor da hipotenusa = %f",hipotenusa);
    return 0;
*/

/*
    float sal,con1,con2,total;
    printf("Digite seu salario e os valores das 2 contas:");
    scanf("%f%f%f%*c",&sal,&con1,&con2);
    con1 = con1 * 1.02;
    con2 = con2 * 1.02;
    total = sal - con1 - con2;
    printf("O que restara e: %.2f",total);
    return 0;
*/

/*
    int ano_nascimento, ano_atual, idade_anos;
    printf("Digite o ano de nascimento:");
    scanf("%d%*c",&ano_nascimento);
    printf("Digite o ano atual:");
    scanf("%d%*c",&ano_atual);
    idade_anos = ano_atual-ano_nascimento;
    printf("\nIdade em anos: %d",idade_anos );
    printf("\nIdade em meses: %d",idade_anos*12);
    printf("\nIdade em dias: %d",idade_anos*365);
    printf("\nIdade em semanas: %d",idade_anos*52);
*/

/*
    int x;
    printf("Digite o valor da tabuada:");
    scanf("%d%*c",&x);
    printf("\n%d x 0 = %d",x,x*0;);
    printf("\n%d x 1 = %d",x,x*1;);
    printf("\n%d x 2 = %d",x,x*2;);
    printf("\n%d x 3 = %d",x,x*3;);
    printf("\n%d x 4 = %d",x,x*4;);
    printf("\n%d x 5 = %d",x,x*5;);
    printf("\n%d x 6 = %d",x,x*6;);
    printf("\n%d x 7 = %d",x,x*7;);
    printf("\n%d x 8 = %d",x,x*8;);
    printf("\n%d x 9 = %d",x,x*9;);
    printf("\n%d x 10 = %d",x,x*10;);
*/

/*
    float sal_min, sal, qtd;
    printf("Digite o valor do salario minimo e o atual:");
    scanf("%f%f%*c",&sal_min,&sal);
    qtd = sal / sal_min;
    printf("\nQtd de salarios min: %.1f",qtd);
*/
    //return 0;
}

//prinf("15%% %.2f",peso*1.15);

/*
int main()
{
    float salario, valor_vend, comissao, sal_final;
    printf("Digite o salario fixo e o valor de vendas:");
    scanf("%f%f%*c",&salario,&valor_vend);
    comissao = valor_vend * 0.04;
    sal_final = salario + comissao;

    printf("Comissao e de: %.2f",comissao);
    printf("O salario final e: %.2f",sal_final);
    return 0;
}
*/

/*
int main()
{
    setlocale(LC_ALL, "portuguese");
    float preço, novopreço;
    printf("Digite o preço do produto:");
    scanf("%f%*c",&preço);
    novopreço = preço * 0.9;
    printf("\nNovo preço: %.2f",novopreco);

    return 0;
}
*/
